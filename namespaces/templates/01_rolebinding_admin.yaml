apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: admin
  namespace: {{ .Values.name }}
roleRef:
  kind: ClusterRole
  apiGroup: authorization.openshift.io
  name: admin
subjects:
- kind: User
  name: {{ .Values.admin }}
userNames:
- {{ .Values.admin }}
